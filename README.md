# SUSHI HOUSE v5

Menu-first mobile UX: the home screen shows large category tiles; categories with subcategories open a second menu level (e.g. Суши → Классические суши), then products. Cart and the existing GitHub/Render deployment flow are preserved.


## v6 UI change
The horizontal category ticker is removed. All menu categories are presented as large cards on the home screen.


## v8 navigation fix
Fixed the home category navigation DOM IDs, recursive category counts, and mobile tap handlers.
